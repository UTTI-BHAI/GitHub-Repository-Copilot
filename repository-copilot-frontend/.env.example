# Base URL of your FastAPI backend.
# In development, requests to /api are proxied to http://localhost:8000 (see vite.config.ts),
# so this can usually be left unset locally.
VITE_API_BASE_URL=http://localhost:8000
